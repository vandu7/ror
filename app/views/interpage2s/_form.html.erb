<%= form_with(model: interpage2, local: true) do |form| %>
  <% if interpage2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(interpage2.errors.count, "error") %> prohibited this interpage2 from being saved:</h2>

      <ul>
      <% interpage2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
