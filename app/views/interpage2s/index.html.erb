<p id="notice"><%= notice %></p>

<h1>Interpage2s</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @interpage2s.each do |interpage2| %>
      <tr>
        <td><%= link_to 'Show', interpage2 %></td>
        <td><%= link_to 'Edit', edit_interpage2_path(interpage2) %></td>
        <td><%= link_to 'Destroy', interpage2, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Interpage2', new_interpage2_path %>
