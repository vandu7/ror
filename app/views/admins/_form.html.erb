<%= form_with(model: admin, local: true) do |form| %>
  <% if admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin.errors.count, "error") %> prohibited this admin from being saved:</h2>

      <ul>
      <% admin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Name %>
    <%= form.text_field :Name, id: :admin_Name %>
  </div>

  <div class="field">
    <%= form.label :Adminid %>
    <%= form.text_field :Adminid, id: :admin_Adminid %>
  </div>

  <div class="field">
    <%= form.label :Password %>
    <%= form.password_field :Password, id: :admin_Password %>
  </div>

  <div class="field">
    <%= form.label :Designation %>
    <%= form.text_field :Designation, id: :admin_Designation %>
  </div>

  <div class="actions">
    <%= form.submit "login" %>
  </div>
<% end %>
