<p id="notice"><%= notice %></p>

<h1>Interpage1s</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @interpage1s.each do |interpage1| %>
      <tr>
        <td><%= link_to 'Show', interpage1 %></td>
        <td><%= link_to 'Edit', edit_interpage1_path(interpage1) %></td>
        <td><%= link_to 'Destroy', interpage1, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Interpage1', new_interpage1_path %>
